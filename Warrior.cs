using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Warrior : BaseRPGClass
{
    /*
    public Warrior(string name, RPGClassType type, RPGStatTier s,
        RPGStatTier a, RPGStatTier v, RPGStatTier e, RPGStatTier d,
        RPGStatTier c, RPGStatTier l, RPGStatTier i, RPGStatTier p,
        List<EquipType> prof, List<BaseSkill> bSkill) : base(name, type, s, a, v, e, d, c, l, i, p,
        prof, bSkill)
    {
        RPGClassName = name;
        rpgClassType = RPGClassType.Pugilist;
        stTier = RPGStatTier.S;
        agTier = RPGStatTier.C;
        viTier = RPGStatTier.B;
        enTier = RPGStatTier.A;
        deTier = RPGStatTier.D;
        chTier = RPGStatTier.D;
        luTier = RPGStatTier.C;
        inTier = RPGStatTier.E;
        peTier = RPGStatTier.C;

        EqProficiencies.Add(EquipType.Sword);
        EqProficiencies.Add(EquipType.Shield);
        EqProficiencies.Add(EquipType.LightArmor);
        EqProficiencies.Add(EquipType.HeavyArmor);

        // RPGClassSkills.Add(gameObject.AddComponent<StdAttack>());
    }

    public override void RPGClassEffects()
    {
        BaseAlly.St.Modify(20);
    }
    */
}
